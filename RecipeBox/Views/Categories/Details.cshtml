@{
  Layout = "_Layout";
}

@model RecipeBox.Models.Category;

<h2>Category Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h3>

@if(@Model.Recipes.Count == 0)
{
  <p>This category does not contain any recipes</p>
}
else
{
  <h4>Recipes the category contains:</h4>
  <ul>
  @foreach(var join in Model.Recipes)
  {
    <li>@join.Recipe.Description</li>
  }
  </ul>
}

<p>@Html.ActionLink("Back to categories", "Index", "Categories")</p>
<p>@Html.ActionLink("Edit Category", "Edit", "Categories", new { id = Model.CategoryId })</p>
<p>@Html.ActionLink("Delete Category", "Delete", "Categories", new { id = Model.CategoryId })</p>